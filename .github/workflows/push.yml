name: PUSH CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        name: checkout
      - name: cache docker image
        uses: actions/cache@v2
        with:
          path: |
            ./docker_image.tar
          key: ${{ runner.os }}-${{ GITHUB.SHA }}-docker-image
      - name: get docker image name
        run: |
          REPO_NAME="$(echo $GITHUB_REPOSITORY | sed 'y/ABCDEFGHIJKLMNOPQRSTUVWXYZ/abcdefghijklmnopqrstuvwxyz/')"
          echo "REPO NAME ($REPO_NAME)" &&
          echo "REPO NAME 2 ($REPO_NAME)"
      - name: build
        run: |
          docker build . -f ./.dockerfile -t abdulghani/go-graphql-test &&
          docker save -o ./docker_image.tar abdulghani/go-graphql-test
